<div class="card col-md-12 text-center">
    <h4>{{title}}</h4>
    <p>{{description}}</p>
    <p>Created by {{user.username}} on {{format_date date_created}}</p>
      <div>
        <button class="btn btn-sm btn-danger" data-id="{{id}}">DELETE POST</button>
      </div>
</div>
